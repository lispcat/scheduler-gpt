# Chatgpt scheduler project

- To run all tests: `cargo test`
- To run with arguments:
```bash
cargo run -- [args] [inputfile]   # compile and run
# or
./target/debug/scheduler-get [args] [inputfile]   # run, don't compile

```
